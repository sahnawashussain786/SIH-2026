# Client environment (Vite) — copy to .env for local overrides.
# Leave empty for local dev (Vite proxies /api to localhost:5000).
#
# Production (Vercel): set VITE_API_URL to your deployed API base, e.g.
#   VITE_API_URL=https://bhoomi-ai-api.vercel.app/api
VITE_API_URL=https://sih-2026-alpha-navy.vercel.app/
